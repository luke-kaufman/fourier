// discrete fourier transform - 
// takes input signal as an array and computes the discrete fourier transform

function DiscreteFourierTransform(values){
    let X = [];
    
    for(let k=0; k<N; k++){
        let real = 0;
        let imaginary = 0;
        for(let n=0; n<N; n++){
            real += ;
            imaginary += ;
        }
    }

    return null; 
}